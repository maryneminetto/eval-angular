import { BordureAuSurvoleDirective } from './bordure-au-survole.directive';

describe('BordureAuSurvoleDirective', () => {
  it('should create an instance', () => {
    const directive = new BordureAuSurvoleDirective();
    expect(directive).toBeTruthy();
  });
});
